describe('Primer test para validar el workflow de GitHub', () => {
    test('Debe confirmar que la suma básica se evalúa correctamente', () => {
        const resultado = 2 + 2;
        expect(resultado).toBe(4); // Verifica que 2 + 2 efectivamente sea igual a 4
    });
});
